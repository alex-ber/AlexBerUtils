[project]
name = "alex-ber-utils"
version = "0.14.0"
requires-python = ">=3.10"
dynamic = ["description", "readme", "optional-dependencies"]

[build-system]
requires = ["setuptools>=80.9.0", "wheel"]
build-backend = "setuptools.build_meta"

[dependency-groups]
dev = [
    "build>=1.5.0",
    "twine>=7.0.0",
]


#uv python pin 3.10